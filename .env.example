# Copy these values into your deployment environment.
PORT=8787
DATA_DIR=./data
SYNC_TOKEN=replace-with-a-long-random-token
# Comma-separated origins, for example:
# CORS_ORIGINS=https://your-workbench.example.com,http://localhost:3000
# For the local file:// preview only, use CORS_ORIGINS=null.
CORS_ORIGINS=
MAX_BODY_BYTES=5242880
